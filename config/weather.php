<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 날씨 제공자
    |--------------------------------------------------------------------------
    |
    | 현재는 API 키 없이 바로 쓸 수 있는 Open-Meteo를 사용한다. 추후 기상청
    | 공공데이터포털 API로 전환하려면 WeatherProvider 인터페이스를 구현한
    | 클래스를 추가하고 이 값을 바꾸면 된다. (AppServiceProvider 참고)
    |
    */
    'driver' => env('WEATHER_PROVIDER', 'open_meteo'),

    /*
    |--------------------------------------------------------------------------
    | 관측/예보 지점 좌표
    |--------------------------------------------------------------------------
    |
    | 서울특별시 은평구 대조동 (대조동주민센터 기준)
    |
    */
    'latitude' => (float) env('WEATHER_LATITUDE', 37.614140),
    'longitude' => (float) env('WEATHER_LONGITUDE', 126.920805),

    'timezone' => 'Asia/Seoul',

];
